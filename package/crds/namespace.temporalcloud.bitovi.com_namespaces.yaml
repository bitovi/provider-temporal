---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: namespaces.namespace.temporalcloud.bitovi.com
spec:
  group: namespace.temporalcloud.bitovi.com
  names:
    categories:
    - crossplane
    - managed
    - temporalcloud
    kind: Namespace
    listKind: NamespaceList
    plural: namespaces
    singular: namespace
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Namespace is the Schema for the Namespaces API. Provisions a
          Temporal Cloud namespace. Regions available in Temporal Cloud: https://docs.temporal.io/cloud/regions.
          Note that regions are prefixed with the cloud provider (aws-us-east-1, not
          us-east-1)'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NamespaceSpec defines the desired state of Namespace
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  acceptedClientCa:
                    description: |-
                      encoded CA cert in PEM format that clients use when authenticating with Temporal Cloud. This is a required field when a Namespace uses mTLS authentication.
                      The Base64-encoded CA cert in PEM format that clients use when authenticating with Temporal Cloud. This is a required field when a Namespace uses mTLS authentication.
                    type: string
                  apiKeyAuth:
                    description: |-
                      (Boolean) If true, Temporal Cloud will enable API key authentication for this namespace.
                      If true, Temporal Cloud will enable API key authentication for this namespace.
                    type: boolean
                  capacity:
                    description: (Attributes) The capacity configuration for the namespace.
                      (see below for nested schema)
                    properties:
                      mode:
                        description: |-
                          (String) The mode of the capacity configuration. Must be one of 'provisioned' or 'on_demand'.
                          The mode of the capacity configuration. Must be one of 'provisioned' or 'on_demand'.
                        type: string
                      value:
                        description: |-
                          (Number) The value of the capacity configuration. Must be set when mode is 'provisioned'.
                          The value of the capacity configuration. Must be set when mode is 'provisioned'.
                        type: number
                    type: object
                  certificateFilters:
                    description: (Attributes List) A list of filters to apply to client
                      certificates when initiating a connection Temporal Cloud. If
                      present, connections will only be allowed from client certificates
                      whose distinguished name properties match at least one of the
                      filters. Empty lists are not allowed, omit the attribute instead.
                      (see below for nested schema)
                    items:
                      properties:
                        commonName:
                          description: |-
                            (String) The certificate's common name.
                            The certificate's common name.
                          type: string
                        organization:
                          description: |-
                            (String) The certificate's organization.
                            The certificate's organization.
                          type: string
                        organizationalUnit:
                          description: |-
                            (String) The certificate's organizational unit.
                            The certificate's organizational unit.
                          type: string
                        subjectAlternativeName:
                          description: |-
                            (String) The certificate's subject alternative name (or SAN).
                            The certificate's subject alternative name (or SAN).
                          type: string
                      type: object
                    type: array
                  codecServer:
                    description: (Attributes) A codec server is used by the Temporal
                      Cloud UI to decode payloads for all users interacting with this
                      namespace, even if the workflow history itself is encrypted.
                      (see below for nested schema)
                    properties:
                      customErrorLink:
                        description: |-
                          (String) A link displayed alongside the custom error message for the codec server.
                          A link displayed alongside the custom error message for the codec server.
                        type: string
                      customErrorMessage:
                        description: |-
                          (String) A custom error message to display when the codec server returns an error.
                          A custom error message to display when the codec server returns an error.
                        type: string
                      endpoint:
                        description: |-
                          (String) The endpoint of the codec server. Must begin with "https".
                          The endpoint of the codec server. Must begin with "https".
                        type: string
                      includeCrossOriginCredentials:
                        description: |-
                          origin credentials in requests to the codec server.
                          If true, Temporal Cloud will include cross-origin credentials in requests to the codec server.
                        type: boolean
                      passAccessToken:
                        description: |-
                          (Boolean) If true, Temporal Cloud will pass the access token to the codec server upon each request.
                          If true, Temporal Cloud will pass the access token to the codec server upon each request.
                        type: boolean
                    type: object
                  connectivityRuleIds:
                    description: |-
                      (Set of String) The IDs of the connectivity rules for this namespace.
                      The IDs of the connectivity rules for this namespace.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  fairness:
                    description: (Attributes) The fairness configuration for the namespace.
                      (see below for nested schema)
                    properties:
                      taskQueueFairnessEnabled:
                        description: |-
                          (Boolean) Flag to enable task queue fairness for the namespace. Defaults to disabled.
                          Flag to enable task queue fairness for the namespace. Defaults to disabled.
                        type: boolean
                    type: object
                  name:
                    description: |-
                      64 characters, start with a letter, contain only lowercase letters, numbers, and hyphens, and not end with a hyphen.
                      The name of the namespace. Must be 2-64 characters, start with a letter, contain only lowercase letters, numbers, and hyphens, and not end with a hyphen.
                    type: string
                  namespaceLifecycle:
                    description: (Attributes) The lifecycle configuration for the
                      namespace. This controls settings like delete protection within
                      Temporal Cloud. (see below for nested schema)
                    properties:
                      enableDeleteProtection:
                        description: |-
                          (Boolean) If true, the namespace cannot be deleted. This is a safeguard against accidental deletion. To delete a namespace with this option enabled, you must first set it to false.
                          If true, the namespace cannot be deleted. This is a safeguard against accidental deletion. To delete a namespace with this option enabled, you must first set it to false.
                        type: boolean
                    type: object
                  regions:
                    description: |-
                      us-east-1, not us-east-1). If two regions are specified, the namespace will be replicated across them in a high availability (HA) configuration. Same-region, multi-region, and multi-cloud HA namespaces are supported. Please note that changing, adding, or removing regions for an existing namespace is not currently supported and the provider will throw an error. For HA namespaces the provider will ignore order changes on regions, which can happen if the namespace fails over.
                      The list of regions where this namespace is available. Must be one or two regions. See https://docs.temporal.io/cloud/regions for a list of available regions and HA options. Note that regions are prefixed with the cloud provider (aws-us-east-1, not us-east-1). If two regions are specified, the namespace will be replicated across them in a high availability (HA) configuration. Same-region, multi-region, and multi-cloud HA namespaces are supported. Please note that changing, adding, or removing regions for an existing namespace is not currently supported and the provider will throw an error. For HA namespaces the provider will ignore order changes on regions, which can happen if the namespace fails over.
                    items:
                      type: string
                    type: array
                  retentionDays:
                    description: |-
                      (Number) The number of days to retain workflow history. Any changes to the retention period will be applied to all new running workflows.
                      The number of days to retain workflow history. Any changes to the retention period will be applied to all new running workflows.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  acceptedClientCa:
                    description: |-
                      encoded CA cert in PEM format that clients use when authenticating with Temporal Cloud. This is a required field when a Namespace uses mTLS authentication.
                      The Base64-encoded CA cert in PEM format that clients use when authenticating with Temporal Cloud. This is a required field when a Namespace uses mTLS authentication.
                    type: string
                  apiKeyAuth:
                    description: |-
                      (Boolean) If true, Temporal Cloud will enable API key authentication for this namespace.
                      If true, Temporal Cloud will enable API key authentication for this namespace.
                    type: boolean
                  capacity:
                    description: (Attributes) The capacity configuration for the namespace.
                      (see below for nested schema)
                    properties:
                      mode:
                        description: |-
                          (String) The mode of the capacity configuration. Must be one of 'provisioned' or 'on_demand'.
                          The mode of the capacity configuration. Must be one of 'provisioned' or 'on_demand'.
                        type: string
                      value:
                        description: |-
                          (Number) The value of the capacity configuration. Must be set when mode is 'provisioned'.
                          The value of the capacity configuration. Must be set when mode is 'provisioned'.
                        type: number
                    type: object
                  certificateFilters:
                    description: (Attributes List) A list of filters to apply to client
                      certificates when initiating a connection Temporal Cloud. If
                      present, connections will only be allowed from client certificates
                      whose distinguished name properties match at least one of the
                      filters. Empty lists are not allowed, omit the attribute instead.
                      (see below for nested schema)
                    items:
                      properties:
                        commonName:
                          description: |-
                            (String) The certificate's common name.
                            The certificate's common name.
                          type: string
                        organization:
                          description: |-
                            (String) The certificate's organization.
                            The certificate's organization.
                          type: string
                        organizationalUnit:
                          description: |-
                            (String) The certificate's organizational unit.
                            The certificate's organizational unit.
                          type: string
                        subjectAlternativeName:
                          description: |-
                            (String) The certificate's subject alternative name (or SAN).
                            The certificate's subject alternative name (or SAN).
                          type: string
                      type: object
                    type: array
                  codecServer:
                    description: (Attributes) A codec server is used by the Temporal
                      Cloud UI to decode payloads for all users interacting with this
                      namespace, even if the workflow history itself is encrypted.
                      (see below for nested schema)
                    properties:
                      customErrorLink:
                        description: |-
                          (String) A link displayed alongside the custom error message for the codec server.
                          A link displayed alongside the custom error message for the codec server.
                        type: string
                      customErrorMessage:
                        description: |-
                          (String) A custom error message to display when the codec server returns an error.
                          A custom error message to display when the codec server returns an error.
                        type: string
                      endpoint:
                        description: |-
                          (String) The endpoint of the codec server. Must begin with "https".
                          The endpoint of the codec server. Must begin with "https".
                        type: string
                      includeCrossOriginCredentials:
                        description: |-
                          origin credentials in requests to the codec server.
                          If true, Temporal Cloud will include cross-origin credentials in requests to the codec server.
                        type: boolean
                      passAccessToken:
                        description: |-
                          (Boolean) If true, Temporal Cloud will pass the access token to the codec server upon each request.
                          If true, Temporal Cloud will pass the access token to the codec server upon each request.
                        type: boolean
                    type: object
                  connectivityRuleIds:
                    description: |-
                      (Set of String) The IDs of the connectivity rules for this namespace.
                      The IDs of the connectivity rules for this namespace.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  fairness:
                    description: (Attributes) The fairness configuration for the namespace.
                      (see below for nested schema)
                    properties:
                      taskQueueFairnessEnabled:
                        description: |-
                          (Boolean) Flag to enable task queue fairness for the namespace. Defaults to disabled.
                          Flag to enable task queue fairness for the namespace. Defaults to disabled.
                        type: boolean
                    type: object
                  name:
                    description: |-
                      64 characters, start with a letter, contain only lowercase letters, numbers, and hyphens, and not end with a hyphen.
                      The name of the namespace. Must be 2-64 characters, start with a letter, contain only lowercase letters, numbers, and hyphens, and not end with a hyphen.
                    type: string
                  namespaceLifecycle:
                    description: (Attributes) The lifecycle configuration for the
                      namespace. This controls settings like delete protection within
                      Temporal Cloud. (see below for nested schema)
                    properties:
                      enableDeleteProtection:
                        description: |-
                          (Boolean) If true, the namespace cannot be deleted. This is a safeguard against accidental deletion. To delete a namespace with this option enabled, you must first set it to false.
                          If true, the namespace cannot be deleted. This is a safeguard against accidental deletion. To delete a namespace with this option enabled, you must first set it to false.
                        type: boolean
                    type: object
                  regions:
                    description: |-
                      us-east-1, not us-east-1). If two regions are specified, the namespace will be replicated across them in a high availability (HA) configuration. Same-region, multi-region, and multi-cloud HA namespaces are supported. Please note that changing, adding, or removing regions for an existing namespace is not currently supported and the provider will throw an error. For HA namespaces the provider will ignore order changes on regions, which can happen if the namespace fails over.
                      The list of regions where this namespace is available. Must be one or two regions. See https://docs.temporal.io/cloud/regions for a list of available regions and HA options. Note that regions are prefixed with the cloud provider (aws-us-east-1, not us-east-1). If two regions are specified, the namespace will be replicated across them in a high availability (HA) configuration. Same-region, multi-region, and multi-cloud HA namespaces are supported. Please note that changing, adding, or removing regions for an existing namespace is not currently supported and the provider will throw an error. For HA namespaces the provider will ignore order changes on regions, which can happen if the namespace fails over.
                    items:
                      type: string
                    type: array
                  retentionDays:
                    description: |-
                      (Number) The number of days to retain workflow history. Any changes to the retention period will be applied to all new running workflows.
                      The number of days to retain workflow history. Any changes to the retention period will be applied to all new running workflows.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.regions is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.regions)
                || (has(self.initProvider) && has(self.initProvider.regions))'
            - message: spec.forProvider.retentionDays is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.retentionDays)
                || (has(self.initProvider) && has(self.initProvider.retentionDays))'
          status:
            description: NamespaceStatus defines the observed state of Namespace.
            properties:
              atProvider:
                properties:
                  acceptedClientCa:
                    description: |-
                      encoded CA cert in PEM format that clients use when authenticating with Temporal Cloud. This is a required field when a Namespace uses mTLS authentication.
                      The Base64-encoded CA cert in PEM format that clients use when authenticating with Temporal Cloud. This is a required field when a Namespace uses mTLS authentication.
                    type: string
                  apiKeyAuth:
                    description: |-
                      (Boolean) If true, Temporal Cloud will enable API key authentication for this namespace.
                      If true, Temporal Cloud will enable API key authentication for this namespace.
                    type: boolean
                  capacity:
                    description: (Attributes) The capacity configuration for the namespace.
                      (see below for nested schema)
                    properties:
                      mode:
                        description: |-
                          (String) The mode of the capacity configuration. Must be one of 'provisioned' or 'on_demand'.
                          The mode of the capacity configuration. Must be one of 'provisioned' or 'on_demand'.
                        type: string
                      value:
                        description: |-
                          (Number) The value of the capacity configuration. Must be set when mode is 'provisioned'.
                          The value of the capacity configuration. Must be set when mode is 'provisioned'.
                        type: number
                    type: object
                  certificateFilters:
                    description: (Attributes List) A list of filters to apply to client
                      certificates when initiating a connection Temporal Cloud. If
                      present, connections will only be allowed from client certificates
                      whose distinguished name properties match at least one of the
                      filters. Empty lists are not allowed, omit the attribute instead.
                      (see below for nested schema)
                    items:
                      properties:
                        commonName:
                          description: |-
                            (String) The certificate's common name.
                            The certificate's common name.
                          type: string
                        organization:
                          description: |-
                            (String) The certificate's organization.
                            The certificate's organization.
                          type: string
                        organizationalUnit:
                          description: |-
                            (String) The certificate's organizational unit.
                            The certificate's organizational unit.
                          type: string
                        subjectAlternativeName:
                          description: |-
                            (String) The certificate's subject alternative name (or SAN).
                            The certificate's subject alternative name (or SAN).
                          type: string
                      type: object
                    type: array
                  codecServer:
                    description: (Attributes) A codec server is used by the Temporal
                      Cloud UI to decode payloads for all users interacting with this
                      namespace, even if the workflow history itself is encrypted.
                      (see below for nested schema)
                    properties:
                      customErrorLink:
                        description: |-
                          (String) A link displayed alongside the custom error message for the codec server.
                          A link displayed alongside the custom error message for the codec server.
                        type: string
                      customErrorMessage:
                        description: |-
                          (String) A custom error message to display when the codec server returns an error.
                          A custom error message to display when the codec server returns an error.
                        type: string
                      endpoint:
                        description: |-
                          (String) The endpoint of the codec server. Must begin with "https".
                          The endpoint of the codec server. Must begin with "https".
                        type: string
                      includeCrossOriginCredentials:
                        description: |-
                          origin credentials in requests to the codec server.
                          If true, Temporal Cloud will include cross-origin credentials in requests to the codec server.
                        type: boolean
                      passAccessToken:
                        description: |-
                          (Boolean) If true, Temporal Cloud will pass the access token to the codec server upon each request.
                          If true, Temporal Cloud will pass the access token to the codec server upon each request.
                        type: boolean
                    type: object
                  connectivityRuleIds:
                    description: |-
                      (Set of String) The IDs of the connectivity rules for this namespace.
                      The IDs of the connectivity rules for this namespace.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  endpoints:
                    description: (Attributes) The endpoints for the namespace. (see
                      below for nested schema)
                    properties:
                      grpcAddress:
                        description: |-
                          (String) The gRPC address for API key client connections (may be empty if API keys are disabled).
                          The gRPC address for API key client connections (may be empty if API keys are disabled).
                        type: string
                      mtlsGrpcAddress:
                        description: |-
                          (String) The gRPC address for mTLS client connections (may be empty if mTLS is disabled).
                          The gRPC address for mTLS client connections (may be empty if mTLS is disabled).
                        type: string
                      webAddress:
                        description: |-
                          (String) The address in the Temporal Cloud Web UI for the namespace
                          The address in the Temporal Cloud Web UI for the namespace
                        type: string
                    type: object
                  fairness:
                    description: (Attributes) The fairness configuration for the namespace.
                      (see below for nested schema)
                    properties:
                      taskQueueFairnessEnabled:
                        description: |-
                          (Boolean) Flag to enable task queue fairness for the namespace. Defaults to disabled.
                          Flag to enable task queue fairness for the namespace. Defaults to disabled.
                        type: boolean
                    type: object
                  id:
                    description: (String) The unique identifier of the namespace across
                      all Temporal Cloud tenants.
                    type: string
                  name:
                    description: |-
                      64 characters, start with a letter, contain only lowercase letters, numbers, and hyphens, and not end with a hyphen.
                      The name of the namespace. Must be 2-64 characters, start with a letter, contain only lowercase letters, numbers, and hyphens, and not end with a hyphen.
                    type: string
                  namespaceLifecycle:
                    description: (Attributes) The lifecycle configuration for the
                      namespace. This controls settings like delete protection within
                      Temporal Cloud. (see below for nested schema)
                    properties:
                      enableDeleteProtection:
                        description: |-
                          (Boolean) If true, the namespace cannot be deleted. This is a safeguard against accidental deletion. To delete a namespace with this option enabled, you must first set it to false.
                          If true, the namespace cannot be deleted. This is a safeguard against accidental deletion. To delete a namespace with this option enabled, you must first set it to false.
                        type: boolean
                    type: object
                  regions:
                    description: |-
                      us-east-1, not us-east-1). If two regions are specified, the namespace will be replicated across them in a high availability (HA) configuration. Same-region, multi-region, and multi-cloud HA namespaces are supported. Please note that changing, adding, or removing regions for an existing namespace is not currently supported and the provider will throw an error. For HA namespaces the provider will ignore order changes on regions, which can happen if the namespace fails over.
                      The list of regions where this namespace is available. Must be one or two regions. See https://docs.temporal.io/cloud/regions for a list of available regions and HA options. Note that regions are prefixed with the cloud provider (aws-us-east-1, not us-east-1). If two regions are specified, the namespace will be replicated across them in a high availability (HA) configuration. Same-region, multi-region, and multi-cloud HA namespaces are supported. Please note that changing, adding, or removing regions for an existing namespace is not currently supported and the provider will throw an error. For HA namespaces the provider will ignore order changes on regions, which can happen if the namespace fails over.
                    items:
                      type: string
                    type: array
                  retentionDays:
                    description: |-
                      (Number) The number of days to retain workflow history. Any changes to the retention period will be applied to all new running workflows.
                      The number of days to retain workflow history. Any changes to the retention period will be applied to all new running workflows.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
